name: Renovate

on:
  workflow_dispatch:
  schedule:
    - cron: "*/15 * * * *"

jobs:
  renovate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Renovate
        uses: renovatebot/github-action@v40.1.10
        with:
          configurationFile: .github/renovate.js
          token: ${{ secrets.GITHUB_TOKEN }}
        env:
          RENOVATE_TOKEN: ${{ secrets.REGISTRY_PASSWORD }}
  